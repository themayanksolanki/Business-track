// Fixed quick-set shown in both the 1:1 call overlay (call-widget) and the
// group meeting room's reaction picker — deliberately not the full chat
// emoji picker (shared/emoji-data.ts), which is far too large for a
// glanceable in-call row of buttons.
export const QUICK_REACTIONS: string[] = ['👍', '❤️', '😂', '🎉', '👏', '🔥'];
